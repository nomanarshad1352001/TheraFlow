import { GalleryPage } from "@/components/GalleryPage";

export const metadata = {
  title: "Gallery",
  description: "A visual reference library of clinic spaces, care teams and calm moments behind TheraFlow.",
};

export default function Page() {
  return <GalleryPage />;
}
