import { AboutPage } from "@/components/AboutPage";

export const metadata = {
  title: "About",
  description: "Why TheraFlow exists and the product principles behind a more thoughtful path to mental healthcare.",
};

export default function Page() {
  return <AboutPage />;
}
