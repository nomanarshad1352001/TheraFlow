import { SecurityPage } from "@/components/SecurityPage";

export const metadata = {
  title: "Security",
  description: "Learn how TheraFlow approaches privacy boundaries, data minimization, access, validation, and production readiness.",
};

export default function Page() {
  return <SecurityPage />;
}
